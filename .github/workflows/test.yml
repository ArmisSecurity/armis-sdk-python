# Based on https://github.com/marketplace/actions/python-poetry-action

name: Run tests

on:
  - push

permissions:
  contents: read

jobs:
  test:
    uses: ./.github/workflows/install.yml@master
    runs-on: ubuntu-latest

    steps:
    - name: Run tests
      run: poetry run pytest -vv
